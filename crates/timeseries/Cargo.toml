[package]
name = "timeseries"
version = "0.1.0"
edition = "2024"

[dependencies.rust_decimal]
workspace = true

[dependencies.resolution]
path = "../resolution"
features = []

[features]
std = ["resolution/std"]
default = ["std"]